defmodule ForthEvaluator.Stack do
  def pop(_stack), do: {:ok, "return value"}

  def push(_stack, _value), do: {:ok, ""}

  def add(_stack), do: {:ok, ""}

  def substract(_stack), do: {:ok, ""}

  def multiply(_stack), do: {:ok, ""}

  def divide(_stack), do: {:ok, ""}

  def duplicate(_stack), do: {:ok, ""}

  def drop(_stack), do: {:ok, ""}

  def swap(_stack), do: {:ok, ""}

  def over(_stack), do: {:ok, ""}
end
